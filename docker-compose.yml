version: '3'

services:
  burrito_server:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 6969:6969
    volumes:
      - $HOME/.ssh:/root/.ssh:ro
    environment:
      - ROCKET_PORT=6969
      - ROCKET_ADDRESS=0.0.0.0

name: burrito_prod

services:
  burrito_server:
    build:
      context: .
      dockerfile: ./docker/prod/Dockerfile
    ports:
      - 6969:6969
    environment:
      - ROCKET_PORT=6969
      - ROCKET_ADDRESS=0.0.0.0

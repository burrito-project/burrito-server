name: Deploy burrito server to VPS
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --all-features

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Pull and deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          debug: true
          envs: REPO_PATH,APP_ENV_FILE
          script: |
            cd $REPO_PATH
            git reset --hard
            git clean -df
            git pull origin $GITHUB_REF
            echo "$APP_ENV_FILE" > .env
            docker compose up -d --build --no-deps --force-recreate --remove-orphans burrito_server
        env:
          REPO_PATH: ${{ secrets.SSH_REPO_PATH }}
          APP_ENV_FILE: |
            ${{ secrets.ENV_FILE }}
